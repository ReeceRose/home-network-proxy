package consts

const (
	// API_URL is a key used to lookup the api url (Used by: reporting-tool)
	API_URL = "API_URL"
	// REPORTING_TOOL_FILENAME is the filename of a file which contains a UUID (Used by: reporting-tool)
	REPORTING_TOOL_FILENAME = "reporting-tool.json"
)
