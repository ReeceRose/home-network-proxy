package consts

const (
	// API_URL is a key used to lookup the api url (Used by: reporting-tool)
	API_URL = "API_URL"
	// REPORTING_TOOL_FILENAME is a key used to lookup the filename which contains a UUID (Used by: reporting-tool)
	REPORTING_TOOL_FILENAME = "REPORTING_TOOL_FILENAME"
)
